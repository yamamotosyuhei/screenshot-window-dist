# スクショ窓口 自動セットアップ手順（AIエージェント用）

⚠ これは人間向けの説明書ではありません（人間向けは「はじめにお読みください_スタートアップガイド.html」）。
**Claude Code などのAIエージェントに、このファイルを読ませてください。**
例：Claude Code のチャットに「このファイルを読んでセットアップして」と書いて、このファイルをドラッグ（またはパスを貼る）。

---

## エージェントへ：あなたがやること

あなた（AIエージェント）が以下を上から順に実行し、セットアップを完了させてください。
人間にやらせてよいのは **ダイアログのボタンを押すことと、スクショを1枚撮ることだけ** です。

前提：
- **このファイルがあるフォルダ（解凍済みの「スクショ窓口配布版」）を基点に作業する**（`cd` してから始める）
- 各ステップの実行前に、何をするか1行で宣言する
- 破壊的コマンドはこの手順に無い（コピー・属性削除・設定追記のみ）。`rm` 系は一切使わないこと

### 1. アプリを /Applications へ入れる

既に `/Applications/スクショ窓口.app` が存在する場合だけ、上書きしてよいか人間に確認する。無ければそのまま：

```bash
ditto "スクショ窓口.app" "/Applications/スクショ窓口.app"
```

### 2. 「開発元が未確認」の警告を出なくする（検疫属性を外す）

```bash
xattr -dr com.apple.quarantine "/Applications/スクショ窓口.app"
```

これで初回の「右クリック→開く」の儀式が不要になる。
（このアプリは配布元がソースからビルドした自作アプリで、Apple有料署名を取っていないだけ。中身はローカル動作のみ・クラウド送信なし）

### 3. エージェント連携CLIを入れる

```bash
echo y | bash "エージェント連携/install.sh"
```

これで次の3つが終わる：
1. `~/bin/` に `lastshot` / `selfshot` / `lastdl` が入る
2. `~/.zshrc` にPATH追記（無ければ）
3. `~/.claude/CLAUDE.md` に使い方スニペットが追記される（既にあればスキップされる）

install.sh の最後に「✓」の行が出ることを確認する。エラーが出たら止まって人間に報告する。

### 4. アプリを起動して、人間にボタンを1つ押してもらう

```bash
open "/Applications/スクショ窓口.app"
```

起動すると同意ダイアログが出るので、人間にこう伝える：

> 画面に「スクショ窓口へようこそ」のダイアログが出ています。**「設定する」を押してください。**
> （スクショの保存先を `~/Pictures/スクショ窓口/` に変える確認です。いつでもメニューの「アンインストール」で元に戻せます）

### 5. 動作確認（あなたが自分でやる）

```bash
defaults read com.apple.screencapture location
```
→ 出力が `…/Pictures/スクショ窓口` になっていればOK（人間が「設定する」を押した後に確認）。

```bash
~/bin/lastshot
```
→ まだスクショが無い場合「スクショ窓口にスクショがありません」で**正常**。

最後に人間に `⌘+Shift+4` でスクショを1枚撮ってもらい、もう一度 `~/bin/lastshot` を実行。
撮ったばかりのファイルのパスと「○秒前」が出れば**セットアップ完了**。

※ `~/bin/selfshot`（AIが自分で画面を撮る）の初回実行時は、macOSが「画面収録」の許可を求めることがある。
その時は人間に：システム設定 → プライバシーとセキュリティ → 画面収録 → ターミナル（またはClaude Codeの実行元アプリ）を許可、と案内する。

### 6. 完了報告

人間に以下を報告して終わる：
- スクショは撮るだけで自動で「窓口」に集まる（メニューバー🖼️ or Dockアイコンで一覧）
- AIに「さっきのスクショ見て」「今の画面見て」「今落としたやつ見て」と言えば、AIが自分で見つけて開く
- 詳しい使い方は同じフォルダの「説明書.html」

---

## トラブル時（エージェント向け）

| 症状 | 対処 |
|:---|:---|
| 撮っても窓口に入らない | `defaults write com.apple.screencapture location ~/Pictures/スクショ窓口` → `killall SystemUIServer` |
| アプリが起動しない | `~/bin/screenshot_window.log` を読む（無ければ人間に報告） |
| 二重起動の警告 | 正常動作（1つだけ起動する設計）。残骸は `pkill -f "スクショ窓口"` 後に再起動 |
| `--win` でエラー | pyobjc未導入。全画面 `selfshot` は使える。必要なら `pip3 install pyobjc` を人間に確認してから |
